{
  "name": "portfolio",
  "version": "0.0.1",
  "dependencies": {
  },
  "scripts": {
    "watch": "concurrently \"npm run watch-ts\" \"npm run watch-sass\"",
    "watch-sass": "sass --watch assets/sass:assets/dist/ ",
    "compile-ts": "tsc",
    "watch-ts": "tsc -w"
  },
  "devDependencies": {
    "concurrently": "^7.2.2",
    "typescript": "^4.7.4",
    "sass": "^1.53.0"
  }
}
